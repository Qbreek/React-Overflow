import './Actions.css';
import { Filters } from './Filters';

export const Actions = () => {
    return (
        <section className="all-questions-actions-container">
            <Filters />
        </section>
    );
};
